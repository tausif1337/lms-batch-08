# Copy this file to .env
# Where the Django backend is running. api.js falls back to this same value
# if the .env file is missing, so the app still works without it.
VITE_API_BASE_URL=http://127.0.0.1:8000/api
